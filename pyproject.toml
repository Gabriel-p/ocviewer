[project]
name = "ocviewer"
version = "0.1.0"
description = "Simple browser app to analyze clusters"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "astropy>=8.0.1",
    "pandas>=3.0.3",
    "plotly>=6.8.0",
    "pyarrow>=24.0.0",
    "streamlit>=1.58.0",
]
